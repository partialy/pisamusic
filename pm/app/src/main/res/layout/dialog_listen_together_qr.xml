<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingHorizontal="20dp"
    android:paddingTop="22dp"
    android:paddingBottom="18dp">

    <TextView
        android:id="@+id/listenTogetherQrTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center"
        android:includeFontPadding="false"
        android:maxLines="2"
        android:textColor="@color/pm_dialog_title"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="一起听房间" />

    <ImageView
        android:id="@+id/listenTogetherQrImage"
        android:layout_width="216dp"
        android:layout_height="216dp"
        android:layout_marginTop="16dp"
        android:contentDescription="@string/listen_together_qr_code"
        android:scaleType="fitCenter" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/listenTogetherQrRoomId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:textColor="@color/pm_dialog_message"
            android:textSize="14sp"
            tools:text="房间号 123456" />

        <ImageButton
            android:id="@+id/listenTogetherQrCopyButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/listen_together_copy_room_id"
            android:padding="8dp"
            android:src="@drawable/ic_copy_24"
            android:tint="@color/pm_dialog_confirm" />
    </LinearLayout>
</LinearLayout>
